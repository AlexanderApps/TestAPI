user_query_description = {
    "name": "filter the {users} by name. case insensitve",
    "limit": "limit the number of {users} returned. default is `100`",
    "sort_by": "a list of columns to sort by",
    "order": "Either `asc` for ascending or `desc` for descending",
    "skip": "skip the first `n` users. default is `0`"
}
